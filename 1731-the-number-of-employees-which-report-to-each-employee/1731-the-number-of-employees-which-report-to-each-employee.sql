# Write your MySQL query statement below
SELECT reports_to as employee_id,(SELECT name from Employees WHERE e.reports_to=employee_id) as name,COUNT(reports_to) as reports_count,ROUND(AVG(age)) as average_age FROM Employees e where reports_to IS NOT NULL  GROUP BY reports_to ORDER BY employee_id;